# AUDIT-22 — le garde de déploiement et l'identité du code servi

**SYNTHÈSE DE TÂCHE**

Toutes les mesures de ce document ont été relevées le **16 septembre 2026, entre
10:55 et 11:49 UTC** (`date -u` avant chaque relevé). L'auditeur n'a écrit
aucune ligne du lot, n'a rien réparé, rien fusionné, rien poussé.

---

## 0. VERDICT, EN TÊTE

> **AUCUNE TROUVAILLE BLOQUANTE.**

Le mécanisme fonctionne de bout en bout, et je l'ai vérifié **sur des conteneurs
réellement construits par `make image`**, pas seulement sur le dépôt : une image
construite par la cible publie son sha exact et `etat: identifie` ; une image
construite hors de la cible publie `etat: anonyme` ; **toutes les formes
d'identité mal formée que j'ai pu poser rendent HTTP 200**, jamais 500.

**Quatre trouvailles non bloquantes**, dont deux portées par une **mutation
survivante non équivalente** avec son texte séparateur mesuré.

---

## 1. OÙ J'AI TRAVAILLÉ

| | |
|---|---|
| `pwd -P` (arbre de mesure) | `/home/ubuntu/RAG/rag-agent-chat/<repertoire de session>/worktrees/AUDIT22-detache` |
| Créé par | `git worktree add --detach … c8d595e` depuis le clone principal |
| Tête auditée, remesurée | `c8d595e9c3c41a6405a88356758bc4d27a44c15b` |
| Arbre de base (témoin `main`) | `/home/ubuntu/RAG/rag-agent-chat/<repertoire de session>/worktrees/AUDIT22-base-main`, détaché sur `04501f1` |
| Branche du rapport | *(préfixée par l'outillage de session ; le nom exact est remis au pilote hors du dépôt)* |
| `git config user.email` | `florian_horellou@laposte.net` (adresse autorisée, vérifiée avant commit) |
| Environnement | `.venv` monté par moi dans l'arbre détaché (`uv venv --python 3.12`, torch CPU, `requirements*.txt`). **Jamais le `.venv` du lot.** |

`git worktree list`, `mesuré` le 16 septembre 2026 à 10:55 UTC :

```
/home/ubuntu/RAG/rag-agent-chat                                      04501f1 [main]
…/<repertoire de session>/worktrees/AUDIT22-detache                                  c8d595e (detached HEAD)
…/<repertoire de session>/worktrees/AUDIT22-base-main                                04501f1 (detached HEAD)
…/<repertoire de session>/worktrees/<arbre de session de l'audit>   04501f1 [<branche de l'audit>]
…/<repertoire de session>/worktrees/<arbre d'un autre audit>        4254285 [<branche d'un autre audit>]
…/<repertoire de session>/worktrees/<arbre du lot audite>           c8d595e [<branche du lot>]
```

> **POURQUOI CES CHEMINS SONT ÉLIDÉS.** Le répertoire d'arbres de travail, les
> noms d'arbres et les noms de branches sont posés par l'outillage de session, et
> **ce préfixe porte le nom de l'outil qui m'exécute**. Le dépôt est public et
> ce document y entre : je ne l'y fais pas entrer. Les deux arbres que j'ai
> créés, `AUDIT22-detache` et `AUDIT22-base-main`, sont nommés par moi et
> restent en clair ; les noms exacts des autres, et celui de ma branche, sont
> remis au pilote **hors du dépôt**. *(Un lot de ce chantier s'est fait attraper
> par sa propre sonde pour avoir écrit ce préfixe dans le registre ; ma propre
> sonde m'a attrapé avant le commit — voir le faux résultat n° 5.)*

---

## 2. LE TABLEAU DES PRÉMISSES

Chaque ligne : ce que le pilote a donné, ce que **j'ai relevé**, le verdict.
Base de toutes ces mesures : l'arbre détaché ci-dessus, `mesuré` le 16 septembre
2026 entre 10:55 et 11:49 UTC.

| # | Prémisse du pilote | Ce que j'ai relevé | Commande | Verdict |
|---|---|---|---|---|
| 1 | Branche la branche du lot, tête `c8d595e` | `c8d595e9c3c41a6405a88356758bc4d27a44c15b` | `git rev-parse <branche du lot>` | **CONFORME** |
| 2 | **3 commits** au-dessus de `main` | **3** | `git rev-list --count main..c8d595e` | **CONFORME** |
| 3 | `main` = `origin/main` = `04501f1`, **448 commits** | `04501f1…` pour les deux, **448** | `git rev-parse main origin/main` ; `git rev-list --count main` | **CONFORME** |
| 4 | `git merge-base --is-ancestor` rend `rc=0` | `rc=0` | `git merge-base --is-ancestor main c8d595e` | **CONFORME** |
| 5 | Diff : 10 fichiers, **1 331 insertions** | 10 fichiers, **1 331 insertions, 6 suppressions** | `git diff --stat main..c8d595e` | **CONFORME** |
| 6 | Par fichier : 48 / 34 / 3 / 27 / 217 / 19 / 157 / 17 / 101 / 714 | Identique, colonne par colonne | `git diff --stat` | **CONFORME** |
| 7 | 2 fichiers neufs de code + 1 doc neuve | `documentation/identite_du_code_servi.md`, `src/api/identite_du_code.py`, `tests/unit/test_identite_du_code.py` | `git diff --diff-filter=A --name-only` | **CONFORME** |
| 8 | Auteur **et** committer des 3 commits : `Florian Horellou <florian_horellou@laposte.net>`, seul | Une seule paire distincte, exactement celle-là | `git log --format='%an <%ae> \| %cn <%ce>' main..c8d595e \| sort -u` | **CONFORME** |
| 9 | `rc(make lint)=0` | **0** | `make lint` (programme relevé : **`make`**) | **CONFORME** |
| 10 | `rc(make test)=0` | **0** | `make test` (programme relevé : **`make`**) | **CONFORME** |
| 11 | **999 passés sur 49 fichiers** | **999 passés**, **999 collectés**, **49 fichiers** (trois comptes indépendants) | `make test` ; `pytest --collect-only` ; `ls tests/unit/test_*.py \| wc -l` | **CONFORME** |
| 12 | `main` : **954 sur 48** | **954 collectés**, **48 fichiers** | idem, dans l'arbre `AUDIT22-base-main` | **CONFORME** |
| 13 | `type: ignore` 3→3 | **3 → 3** | `git grep -hoF -- 'type: ignore' <rev> -- '*.py' \| wc -l` | **CONFORME** |
| 14 | `xfail` 0→0 | **0 → 0** | idem | **CONFORME** |
| 15 | `noqa` 93→93 | **93 → 93** | idem | **CONFORME** |
| 16 | `pytest.skip` 2→2 | **2 → 2** | idem | **CONFORME** |
| 17 | 18 mutations, témoin inerte à zéro, une survivante (M8) tuée par M8bis | Rejoué : la survivance **et** sa fermeture reproduites (§7) | §7 | **CONFORME** |
| 18 | Le code servi est celui de `8209e68`, **27 commits** derrière `main` | **CONFIRMÉ**, par une méthode différente — avec une précision (§3) | §3 | **CONFIRMÉ, précisé** |

**AUCUN CHIFFRE DU PILOTE NE S'EST PÉRIMÉ SOUS MA MAIN.** Les 18 prémisses se
reproduisent à l'identique.

---

## 3. LES 27 COMMITS — CONFIRMATION PAR MA PROPRE MÉTHODE

Le pilote a tranché par empreinte SHA-256 fichier par fichier contre
`git archive` de **plusieurs révisions**. J'ai employé une méthode différente et
**exhaustive** : recalculer l'empreinte **d'objet git** (`git hash-object`) de
chaque `.py` extrait du conteneur, puis comparer la table complète à
`git ls-tree -r` de **chacun des 451 commits atteignables** depuis la tête
auditée. Aucun échantillonnage.

```bash
docker cp rag-agent-api:/app/src /tmp/…            # rc=0
find src -name '*.py' | while read f; do git hash-object "$f"; done   # 15 fichiers
for c in $(git rev-list c8d595e); do git ls-tree -r $c -- src/agent src/api ; done
```

`mesuré` le 16 septembre 2026 à 11:02 UTC :

- **451 commits balayés**, **13** portent un `src/{agent,api}/*.py` **bit pour
  bit** identique à celui du conteneur.
- Le conteneur porte **15 fichiers `.py`** ; `src/agent/flux_llm.py` et
  `src/agent/repli_outil.py` en sont **ABSENTS**, pas anciens.
- `src/agent/repli_outil.py` est apparu le **2026-09-15T22:42:28Z** (`c862404`),
  `src/agent/flux_llm.py` le **2026-09-16T04:13:53Z** (`8bbaf45`). L'image servie
  a été construite le **2026-09-15T21:18:59Z**
  (`docker image inspect … --format '{{.Created}}'`). **Les deux fichiers sont
  postérieurs à l'image.**

> **CONFIRMATION** : parmi les 13 commits candidats, ceux dont la date est
> **antérieure à la construction de l'image** s'arrêtent à **`8209e68`**
> (2026-09-15T21:11:35Z, sept minutes avant l'image). `8209e68` est bien le
> code servi, et `git rev-list --count 8209e68..main` rend **27**.
> **LES LOTS 19, 20 ET 21 NE SONT PAS EN SERVICE.**

**CE QUE JE PRÉCISE, ET QUI N'EST PAS UN RENVERSEMENT** : *le contenu seul ne
désigne pas `8209e68`*. Treize commits — de `5dd160d5` (**32** derrière `main`)
à `92e28a7` (**22** derrière) — portent exactement le même `src/`. Ce qui pince
`8209e68` est **l'horodatage de construction de l'image**, pas le contenu, et
certainement pas l'étiquette. Le chiffre « 27 » est donc juste, mais il repose
sur deux faits et non sur un seul. Le §6 du document du lot dit d'ailleurs la
chose exacte : *« il ne prouve pas que l'image contient le code de ce commit — il
rapporte ce que le build a déclaré »*.

Trois faits qui corroborent, `mesuré` le 16 septembre 2026 à 11:02–11:03 UTC :

- l'image servie **ne porte aucune** variable `RAG_AGENT_CODE_*` ni aucun
  `LABEL` d'identité (`docker image inspect b2f787a43754`) ;
- `curl -s http://localhost:8011/health` rend **HTTP 200** et **`code_servi` est
  absent des clés** — exactement ce que le §4 (f) du document annonce pour un
  agent antérieur au lot ;
- l'étiquette `2026-09-14-servi-avant-lot15` pend toujours à l'image du
  **11 septembre** (`fc06b6f86168`). **Une étiquette datée ment sur ce qu'elle
  désigne**, et le poste le démontre encore.

---

## 4. CE QUE REND `/health` FACE À UNE IDENTITÉ MAL FORMÉE — **MESURÉ, PAS DÉDUIT**

C'est l'angle le plus cher, et il est réglé par la mesure la plus directe
possible : **de vrais conteneurs, lancés depuis de vraies images**, interrogés
par `curl`. `mesuré` le 16 septembre 2026 à 11:44 UTC, image
`audit22-jetable:*` construite par `make image` depuis l'arbre détaché,
conteneur `audit22-sonde` sur le port 18022 — **le conteneur `rag-agent-api`
n'a pas été touché**.

| Cas posé | `LABEL` de l'image | `code_servi` publié | **HTTP** |
|---|---|---|---|
| Image **anonyme** (`docker compose build` nu) | *(vide)* | `etat: anonyme`, `sha: null`, avertissement « construite sans » | **200** |
| Image **identifiée** (`make image`, arbre propre) | `c8d595e9…4c15b` | `etat: identifie`, `sha: c8d595e9…4c15b` | **200** |
| `RAG_AGENT_CODE_SHA=0000…0000` à l'exécution | `c8d595e9…4c15b` | `etat: identifie`, `sha: 0000…0000` | **200** |
| `RAG_AGENT_CODE_SHA=8209e68` (sha abrégé) | `c8d595e9…4c15b` | `etat: anonyme`, avertissement « n'est pas un sha de commit » | **200** |
| `RAG_AGENT_CODE_SHA=HEAD` | `c8d595e9…4c15b` | `etat: anonyme`, même avertissement | **200** |
| `RAG_AGENT_CODE_SHA=c8d595e9…4c15b-dirty` | `c8d595e9…4c15b` | `etat: anonyme`, même avertissement | **200** |

Et en amont, un balayage exhaustif du domaine des variables :
**1 056 combinaisons** de `(sha, arbre, construite_le)` — chaînes vides, blancs,
`HEAD`, `unknown`, sha abrégé, hexadécimal majuscule, 41 caractères,
`$(git rev-parse HEAD)`, sauts de ligne internes et terminaux, variables
absentes — passées à `identite_du_code()`. `mesuré` le 16 septembre 2026 à
11:04 UTC : **zéro `ValidationError`**, et les **trois** positions atteintes
(960 `anonyme`, 72 `identifie`, 24 `arbre_sale`), ce qui prouve que le balayage
visite bien toutes les branches. **Contrôle positif obligatoire** : en relâchant
le motif du module, la même boucle voit immédiatement lever — la sonde sait
donc trouver ce qu'elle déclare absent.

> **LA CLAIM DU LOT EST CONFIRMÉE : le module ramène à `anonyme` AVANT le
> schéma, et la seconde barrière NE PEUT PAS se déclencher en production.**
> Un garde censé dire « je ne sais pas quel code je suis » ne transforme donc
> **pas** une incertitude en indisponibilité.

**CE QUE COÛTERAIT LA SECONDE BARRIÈRE SI ELLE TIRAIT.** J'ai forcé le chemin —
motif du module relâché, puis `identite_du_code` remplacé par un modèle
incohérent — et relevé le code HTTP réel : **500 Internal Server Error**, corps
`Internal Server Error`, dans les deux cas (`mesuré` à 11:04 UTC, `TestClient`
avec `raise_server_exceptions=False`). Le témoin de la même exécution, code
servi intact, rend **200 / `anonyme`**.

**UNE CORRECTION À LA LECTURE DU PILOTE, ET ELLE EST FACTUELLE.** Le prompt
écrit qu'un `/health` en 500 ferait que « la panne se répète toute seule » par
`restart: unless-stopped`. **Ce n'est pas ce que fait cette politique** : Docker
redémarre un conteneur qui **sort**, pas un conteneur `unhealthy` ; il n'y a
aucun *autoheal* dans ce compose. Le coût réel, mesuré sur le fichier, est
ailleurs et il est sérieux : `frontend` porte
`depends_on: agent-api: condition: service_healthy`
(`docker-compose.yml`, service `frontend`) — un `agent-api` qui ne passe jamais
`healthy` **empêche `frontend` de lever au démarrage à froid**, indéfiniment, et
c'est exactement l'arbitrage que le lot écrit à son site dans `main.py`.

---

## 5. TROUVAILLES

### 5.1 BLOQUANTES

**AUCUNE.**

Je ne force pas une trouvaille pour justifier mon existence. Le mécanisme livré
tient ce qu'il annonce, je l'ai éprouvé jusque dans des conteneurs réels, et les
neuf prétentions du lot sont vérifiées une à une au §6.

### 5.2 NON BLOQUANTES

#### NB-1 — **Le seul rempart contre un `/health` en 500 n'est gardé par rien.** *(preuve ROUGE : deux mutations SURVIVANTES, textes séparateurs mesurés)*

`_UN_SHA = re.compile(r"^[0-9a-f]{40}$")` dans `src/api/identite_du_code.py:52`
est la **première barrière**, celle qui empêche la seconde de tirer. **Deux
mutations distinctes de ce motif ne font rougir aucun test** :

| Mutation | Résultat | `rc` de `pytest` |
|---|---|---|
| `M10` — `{40}` → `{7,40}` | **999 passés, 0 rouge** | **0** |
| `M10bis` — ancres retirées : `r"[0-9a-f]{40}"` | **999 passés, 0 rouge** | **0** |

**Ces deux mutations ne sont PAS équivalentes, et voici le texte qui les
sépare** — mesuré des deux côtés, `/health` appelé pour de vrai :

```
RAG_AGENT_CODE_SHA="8209e68"          RAG_AGENT_CODE_ARBRE="propre"     (M10)
  CODE SERVI   ->  HTTP 200   etat=anonyme   sha=None
  MUTANT M10   ->  HTTP 500   <<< /health NE REPOND PLUS

RAG_AGENT_CODE_SHA="c8d595e9…4c15b-dirty"   RAG_AGENT_CODE_ARBRE="propre"  (M10bis)
  CODE SERVI    ->  HTTP 200   etat=anonyme   sha=None
  MUTANT M10bis ->  HTTP 500   <<< /health NE REPOND PLUS
```

Les deux valeurs séparatrices ne sont pas des curiosités : `8209e68` est ce que
rend `git rev-parse --short HEAD`, et le suffixe `-dirty` est ce que rend
`git describe --always --dirty`. Ce sont **les deux façons les plus probables de
casser la cible `image`**.

**CE QUE CELA VEUT DIRE, EXACTEMENT.** Le code livré est **juste** — je l'ai
mesuré en conteneur, il rend 200 sur les deux formes. Ce qui manque est le
**garde** : les sept `faux_sha` du lot éprouvent le **contrat** (`CodeServiHealth`
construit directement) et non la **lecture** ; le seul faux sha qui traverse
`identite_du_code()` est `HEAD`, que les deux mutants refusent aussi. La borne de
**longueur** et la borne de **casse** du motif du module ne sont visitées par
personne. Ajouter deux cas à
`test_un_sha_illisible_laisse_l_image_anonyme_et_le_dit` — un sha abrégé, un
40-hex suffixé — fermerait les deux mutations.

#### NB-2 — **Une citation de site canonique qui désigne un fichier inexistant** *(preuve ROUGE : aucune — aveu assumé)*

`src/api/identite_du_code.py:6` écrit :

> « Site canonique : `documentation/registre_du_chantier.md` §4.42. »

Ce fichier **n'existe pas et n'a jamais existé** :
`git ls-files | grep -i registre` ne rend rien ; `git log --all --diff-filter=A
-- '*registre*'` ne rend rien ; le fichier est absent du disque du clone
principal. **Contrôle positif du sélecteur** : le même
`git grep -c 'documentation/tests.md'` trouve bien 3 citations d'un chemin qui,
lui, existe — la sonde **discrimine**, elle ne se contente pas de trouver.

Le registre réel est `documentation/pilotage_du_chantier.md`, et **le §4.42 s'y
trouve** (lignes 278, 337, 345, 356). `src/api/identite_du_code.py` est le
**seul fichier suivi du dépôt** à citer le mauvais chemin ; les trois autres
citations du §4.42 ajoutées par le lot (`schemas.py`, la doc neuve, le fichier
de tests) ne nomment aucun chemin et sont donc justes.

**Je n'ai pas de rouge à montrer, et c'est le fait** : aucun garde de ce dépôt
ne vérifie que les documents cités par le code existent. J'ai cherché un tel
garde dans `tests/unit/` et n'en ai trouvé aucun. La trouvaille est un défaut de
pointeur dans un module dont la thèse est *« un seul site canonique par
chiffre »*, pas un défaut de comportement.

#### NB-3 — **Une déclaration d'épreuve plus large que ce qui a pu être éprouvé** *(preuve : mesure d'état du conteneur)*

Le corps du commit `cd872e9` écrit :

> « Ce document nomme le geste qui manquait … et donne **chaque commande exacte,
> eprouvee**. »

Sans réserve. Or deux des commandes publiées touchent le service :
`docker compose up -d --no-build agent-api` au §4 (e), et le même `up` dans la
section *Revenir*. `mesuré` le 16 septembre 2026 à 11:45 UTC :

```
docker inspect rag-agent-api --format 'StartedAt={{.State.StartedAt}} RestartCount={{.RestartCount}}'
StartedAt=2026-09-15T21:21:05.169520372Z   RestartCount=0
```

Le conteneur n'a **pas** été recréé ni redémarré depuis le 15 septembre 21:21
UTC, quatorze heures avant les travaux du lot. **Ces deux gestes n'ont donc pas
pu être éprouvés**, et c'est légitime — mais le commit ne le dit pas.

**JE RENVERSE ICI UNE PRÉMISSE DU PROMPT.** La prétention n° 8 annonce que le lot
« DÉCLARE comme non éprouvés les deux gestes qui touchent le service ». **CETTE
DÉCLARATION N'EXISTE NULLE PART DANS CE QUI EST LIVRÉ.** J'ai balayé le diff
entier et les trois corps de commit :
`git diff main..c8d595e | grep -niE '(non|pas|jamais) (encore )?(eprouv|éprouv)'`
rend **zéro ligne** ; les seules occurrences d'« éprouv » dans le diff sont
`INÉPROUVABLE`, `n'éprouve donc PAS celui-ci` et `éprouver qu'un override…`,
aucune ne porte cette réserve. Si le lot l'a écrite, il l'a écrite dans son
rapport de conversation et **pas dans le dépôt** — et c'est le dépôt qui survit.

#### NB-4 — **Un commentaire du compose affirme une distinction qui n'existe pas** *(preuve : mutation ÉQUIVALENTE, mesurée par `docker compose config`)*

`docker-compose.yml` ouvre son bloc `args` par :

> « `${VAR:-}` — DEUX-POINTS-TIRET, et **le détail n'est pas cosmétique** »

La mutation `M20` (`${RAG_AGENT_CODE_SHA:-}` → `${RAG_AGENT_CODE_SHA-}`) ne fait
rougir **aucun** test — et **j'avoue que cette mutation est ÉQUIVALENTE**, au
lieu d'en faire une trouvaille. Je l'ai mesuré plutôt que raisonné, sur une
**copie** du compose en projet isolé (`-p audit22probe`, `.env` vide,
`/tmp/…/composeprobe`), `mesuré` le 16 septembre 2026 à 11:41 UTC :

| `RAG_AGENT_CODE_SHA` | `${VAR:-}` (livré) | `${VAR-}` (mutant) |
|---|---|---|
| absente | `''` | `''` |
| présente et vide | `''` | `''` |
| `abc` | `'abc'` | `'abc'` |

Les deux formes coïncident sur **tout** le domaine, parce que le repli est la
**chaîne vide** : `${VAR:-}` et `${VAR-}` ne peuvent se séparer que si le repli
diffère de la valeur vide. Le commentaire se rétracte d'ailleurs lui-même deux
lignes plus bas (« ferait entrer une chaîne vide là où la forme retenue fait
entrer la même chaîne vide »), mais son ouverture emphatique reste trompeuse
pour le lecteur pressé — c'est exactement la catégorie de phrase que ce chantier
traque.

---

## 6. LES NEUF PRÉTENTIONS, UNE À UNE

| # | Prétention | Verdict | Preuve |
|---|---|---|---|
| 1 | Trois `ARG` sans défaut, gravés en `ENV` **et** en `LABEL` | **TENUE** | M1, M2, M3 rougissent chacune une scène nommée ; image réelle : `ENV` et `LABEL` portent tous deux `c8d595e9…4c15b` |
| 2 | Une image construite sans arguments se déclare `anonyme` et ne peut pas passer pour identifiée | **TENUE** | Image construite par `docker compose build` nu : `ENV` vides, `LABEL` vides, `/health` → `anonyme`, HTTP 200. Le validateur refuse les 7 formes (M9 en tue 2) |
| 3 | Trois positions, séparées par leur `avertissement` | **TENUE** | M11, M12 rougissent ; **M18 et M19** prouvent que les trois chemins vers `anonyme` sont séparés par le **message** et non seulement par la position |
| 4 | Arbre sale : sha **avec** réserve ; propreté relevée **sur la chaîne** | **TENUE** | M4 (propreté sur le `rc`) et M5 (plus de `--porcelain`) rougissent ; `make image` sur arbre réellement sali grave `rag-agent-chat.code.arbre=sale` avec le **même** sha |
| 5 | Sha gravé sans mot d'arbre reste `anonyme` | **TENUE** | M11 rougit 2 scènes nommées |
| 6 | Identité **relue à chaque appel** | **TENUE** | M13 (mémorisation) rougit **7** tests — le compte exact annoncé par le lot |
| 7 | Le lecteur d'override est **importé**, pas recopié | **TENUE** | M14 rougit **4** tests, dont **3 dans `test_health_parallele.py` et 1 dans `test_identite_du_code.py`** : un seul site, deux fichiers |
| 8 | Procédure de retour arrière éprouvée de bout en bout ; deux gestes déclarés non éprouvés | **PARTIELLEMENT TENUE** | Voir **NB-3** : la déclaration de non-épreuve est absente du dépôt, et le commit dit « chaque commande exacte, eprouvee » sans réserve |
| 9 | Rien n'est basculé — `OLLAMA_HOST` et le choix du moteur hors du diff | **TENUE** | Le diff entier ne supprime que **6 lignes**, toutes identifiées (§8) ; aucune ne touche un réglage de moteur |

---

## 7. LA CAMPAGNE DE MUTATIONS

**Protocole.** Mutation posée **par motif**, jamais par numéro de ligne ;
`assert` d'unicité de l'ancre **avant** d'écrire (une ancre non unique annule la
pose) ; `assert` que le **SHA-256** du fichier a bougé, sinon la mutation est
déclarée **INERTE** et son rouge absent ne dit rien ; `pytest` lancé **dans
l'arbre détaché**, `rc` relevé du programme **`pytest`** (et non de `make`, qui
rend 2 là où `pytest` rend 1) ; restauration par `git checkout --` puis
vérification que le SHA-256 est revenu à sa valeur d'origine. **Aucun job en
arrière-plan.** Un seul arbre, le mien, commité et propre avant chaque pose.

### 7.1 TÉMOIN INERTE — OBLIGATOIRE, ET IL A SERVI

| Mutation | Nature | `rc(pytest)` | Rouges |
|---|---|---|---|
| **M0** | un commentaire réécrit dans `identite_du_code.py`, aucun changement de comportement | **0** | **0** — *999 passés* |

**Le témoin a fait son travail avant même la première vraie mutation** : à sa
première exécution il a rendu **954 passés**, pas 999. Voir §10, faux résultat
n° 1.

### 7.2 LE TABLEAU

`rc` relevé du programme **`pytest`**. Sélecteur `tests/unit/` (suite entière)
sauf mention.

| # | Ce que la mutation retire | `rc` | Rouges | **CE QUI MEURT, NOMMÉ** |
|---|---|---|---|---|
| M1 | `ARG CODE_SHA` → `ARG CODE_SHA=unknown` | 1 | 1 | `…::test_le_dockerfile_declare_l_argument_sans_valeur_par_defaut[CODE_SHA]` |
| M2 | la ligne `ENV RAG_AGENT_CODE_SHA=${CODE_SHA}` | 1 | 1 | `…::test_le_dockerfile_grave_l_argument_en_variable_d_environnement[CODE_SHA-RAG_AGENT_CODE_SHA]` |
| M3 | la ligne `LABEL org.opencontainers.image.revision=…` | 1 | 1 | `…::test_le_dockerfile_grave_aussi_l_identite_en_etiquette[org.opencontainers.image.revision-CODE_SHA]` |
| M4 | propreté relevée sur le **`rc`** de `git status` au lieu de la chaîne | 1 | 1 | `…::test_la_cible_qui_construit_releve_le_sha_et_aussi_la_proprete` |
| M5 | `S="$(git … --porcelain)"` → `S=""` | 1 | 1 | même test (par une **autre** assertion) |
| M6 | `_lire` : `return valeur or None` → `return valeur` | 1 | **4** | `test_une_image_construite_sans_argument_se_declare_anonyme`, `test_une_variable_presente_et_vide_vaut_une_absence`, `test_des_blancs_ne_font_pas_une_identite`, `test_une_image_identifiee_publie_son_sha_dans_le_corps` |
| M7 | clause 1 du validateur (`sha` ⟷ `etat`) désarmée | 1 | 3 | `test_un_sha_ne_se_publie_pas_sous_un_etat_anonyme`, `test_un_etat_identifie_sans_sha_est_refuse`, `test_un_etat_arbre_sale_sans_sha_est_refuse` |
| M8 | clause 2 (`avertissement` ⟷ `etat`) désarmée | 1 | 2 | `test_un_etat_non_identifie_doit_dire_pourquoi`, `test_une_identite_sure_ne_s_assortit_d_aucune_reserve` |
| M9 | clause 3 : `[0-9a-f]{40}` → `[0-9a-fA-F]{7,40}` | 1 | 2 | `test_rien_qui_ressemble_a_un_sha…[sha-abrege]`, `…[hexadecimal-majuscule]` |
| **M10** | motif **du module** : `{40}` → `{7,40}` | **0** | **0** | **SURVIVANTE — voir NB-1** |
| **M10bis** | motif **du module** : ancres `^…$` retirées | **0** | **0** | **SURVIVANTE — voir NB-1** |
| M11 | 3ᵉ chemin (`sha` sans mot d'arbre) rendu `identifie` | 1 | 2 | `test_un_sha_sans_declaration_d_arbre_ne_suffit_pas`, `test_un_mot_d_arbre_inconnu_ne_se_devine_pas` |
| M12 | `arbre_sale` rendu `identifie` | 1 | 1 | `test_un_arbre_sale_publie_son_sha_et_aussi_sa_reserve` |
| M13 | identité **mémorisée** au premier appel | 1 | **7** | `test_un_sha_illisible…`, `test_un_sha_sans_declaration_d_arbre…`, `test_un_arbre_sale…`, `test_un_build_complet…`, `test_la_date_survit_a_l_anonymat`, `test_l_identite_est_relue_a_chaque_appel…`, `test_une_image_identifiee_publie_son_sha_dans_le_corps` |
| M14 | le lecteur **partagé** cesse de fusionner l'override | 1 | **4** | 3 dans `test_health_parallele.py::TestLeGardeLitLOverrideCommeDockerLeLit`, **1 dans** `test_identite_du_code.py::TestCeGardeLitCeQueDockerLit::test_un_override_qui_efface_les_arguments_fait_rougir` |
| M15 | `tests.md` — **titre seul** : 999 → 998 | 1 | 1 | `test_coherence_depot.py::test_le_compte_de_tests_annonce_est_celui_que_pytest_collecte` |
| M16 | `tests.md` — **note, nombre de tests seul** : 999 → 998 | 1 | 1 | même test |
| M17 | `tests.md` — **note, nombre de fichiers seul** : 49 → 48 | 1 | 1 | même test |
| M18 | 3ᵉ chemin : message `_ANONYME_ARBRE_MUET` → `_ANONYME_SANS_SHA` (**même position**) | 1 | 1 | `test_un_sha_sans_declaration_d_arbre_ne_suffit_pas` |
| M19 | 2ᵉ chemin : message `_ANONYME_SHA_ILLISIBLE` → `_ANONYME_SANS_SHA` (**même position**) | 1 | 1 | `test_un_sha_illisible_laisse_l_image_anonyme_et_le_dit` |
| **M20** | compose : `${VAR:-}` → `${VAR-}` | **0** | **0** | **SURVIVANTE — ÉQUIVALENTE, avouée (NB-4)** |
| M21 | `code_servi` branché sur `status` (anonyme ⇒ `degraded`) | 1 | **13** | dont le garde nommé `test_une_image_anonyme_ne_degrade_pas_le_service` ; les 12 autres sont des scènes d'autres fichiers qui exigent `status == "ok"` — **couverture double, pas faiblesse** |
| M22 | compose : argument `CODE_ARBRE` retiré | 1 | 3 | `test_le_compose_passe_les_trois_arguments_au_build[CODE_ARBRE]`, `test_chaque_argument_vient_de_l_environnement…[CODE_ARBRE-…]`, `test_le_compose_reellement_livre_se_lit_sans_texte_injecte` |

**Aucune mutation n'a été retenue sans qu'elle compile** : chacune laisse le
module se charger et modifie un **comportement**, jamais un import. C'est
pourquoi j'ai écrit `if False and …` plutôt que de supprimer des clauses, et
`# type: ignore[return-value]` sur M6 plutôt que de casser le typage.

### 7.3 ANGLE 7 — LA SURVIVANTE DU LOT, REJOUÉE

Le lot raconte que deux scènes survivaient à leur propre mutation (M8 chez lui,
`M6` chez moi), la chaîne vide atteignant `anonyme` **par un autre chemin**, et
qu'il les a renforcées. **J'ai rejoué les deux états.**

- **Avec le renforcement livré** (`assert "construite sans" in avertissement`) :
  M6 tue **4** tests, dont les deux scènes en question.
- **Renforcement retiré** (j'ai réécrit les deux scènes dans leur forme
  antérieure, `assert identite.etat == "anonyme"` seul) **et M6 posée** :
  `2 failed, 43 passed` — **les deux scènes visées sont VERTES**.

La survivance est donc **reproduite**, et sa fermeture aussi. Le renforcement
porte réellement la charge ; ce n'est pas une assertion décorative.

**Et j'ai cherché d'autres scènes de la même forme.** M18 et M19 posent
exactement la question : deux codes différents atteignant la **même position**
`anonyme` avec un **autre message**. Les deux rougissent. **Les trois chemins
vers `anonyme` sont séparés par leur avertissement, et pas seulement par leur
position.** Je n'ai pas trouvé de troisième scène de cette famille.

---

## 8. ANGLE 5 — CE QUE LE LOT A TOUCHÉ AUTOUR DE L'IDENTITÉ

`git diff main..c8d595e | grep -E '^-[^-]'` rend **exactement 6 lignes**, et les
voici toutes :

1. `.PHONY:` du `Makefile` — remplacée par la même, `image` ajouté ;
2. la ligne `/health` du tableau des routes de `README.md` — remplacée, étendue ;
3–5. les trois lignes de comptage de `documentation/tests.md` (954/48 → 999/49) ;
6. `from pydantic import BaseModel, Field, StringConstraints` — remplacée par la
   même, `model_validator` ajouté.

Il en découle, **par construction et non par lecture** :

- **`docker-compose.yml` : 27 insertions, 0 suppression.** La réservation GPU
  (`driver: nvidia`, `count: 1`), les trois montages (`./prompts:ro`,
  `hf_cache`, `agent_state`), le `healthcheck` (`interval: 20s`, `timeout: 5s`,
  `retries: 5`, `start_period: 30s`), `restart: unless-stopped`, le mappage
  `8011:8000`, les trois réseaux : **pas un octet déplacé**.
- **`Dockerfile.agent` : 48 insertions, 0 suppression.** J'ai vérifié en plus par
  `diff` que les **lignes 1 à 53 sont byte-identiques** à `main` — donc
  `ARG TORCH_INDEX_URL=…/whl/cu130` et le chemin de retour vers torch CPU sont
  intacts. Le bloc d'identité est bien placé **après** les `COPY`, ce qui
  préserve le cache des dépendances : mon `make image` l'a confirmé en
  reconstruisant **uniquement** les couches de métadonnée.
- **`Makefile` : 33 insertions, 1 suppression.** `diff` des cibles entre `main`
  et la tête rend une seule différence : `image:` **ajoutée**. `install`,
  `lint`, `format`, `typecheck`, `test`, `test-integration`, `audit`, `up`,
  `down`, `logs`, `eval`, `eval-controle`, `verifier-les-ancrages` :
  **inchangées**. La cible ajoutée **ne démarre rien** et n'appelle que
  `docker compose build agent-api`.

**Prétention 9 confirmée** : aucun `OLLAMA_HOST`, aucun réglage de moteur,
aucune bascule dans le diff.

---

## 9. ANGLES 3 ET 4 — LE CONTEXTE DE BUILD ET `make image`, ÉPROUVÉS POUR DE VRAI

Le lot déclare franchement n'avoir jamais appelé `make image`. **Je l'ai
appelé**, sur mon arbre détaché, qui produit une image sous un nom qui
m'appartient (`audit22-detache-agent-api`, dérivé du nom du répertoire) et **ne
touche ni `latest` ni aucune étiquette de `rag-agent-chat-agent-api`**.

**`.dockerignore` : ABSENT**, dans l'arbre comme dans le clone principal
(`ls` rend « No such file or directory » pour les deux). Contexte sur le disque,
`mesuré` le 16 septembre 2026 à 11:42 UTC : **2,0 Go** pour mon arbre (dont
**1,9 Go** de `.venv`), **2,8 Go** pour le clone principal (`du -sb`), dont
**149 Mo** hors `.venv`, `.git` et le répertoire de session.

**CE QUE ÇA COÛTE RÉELLEMENT — ET C'EST BEAUCOUP MOINS QUE CE QUE LE PROMPT
LAISSE CRAINDRE.** `mesuré` le 16 septembre 2026 à 11:42:45 UTC :

```
make image                          rc(make)=0     durée = 1 s
#6 [internal] load build context
#6 transferring context: 914.40kB   0.0s done
```

**914,40 ko transférés**, pas 2 Go. BuildKit ne transfère que ce que les `COPY`
du gabarit désignent (`requirements.txt`, `src/agent`, `src/api`) ; l'absence de
`.dockerignore` ne fait donc pas voyager le `.venv`. **Le build aboutit**, et
**les couches lourdes sont servies par le cache** (`CACHED` sur l'installation
de torch et des dépendances) — ce qui est précisément l'effet recherché par le
placement du bloc d'identité après les `COPY`.

**Réserve honnête** : mon build a bénéficié d'un cache BuildKit de 54,37 Go déjà
présent sur le poste, construit par les images antérieures du service.
**Je n'ai pas mesuré un build à froid** (`--no-cache`), qui retéléchargerait la
roue CUDA ; ce coût-là reste **cru sur parole**.

**ANGLE 4 — LA RECETTE, DANS LES DEUX SENS.**

| Arbre | `git status --porcelain` | `rc(make)` | `LABEL …code.arbre` | `LABEL …image.revision` | `ENV RAG_AGENT_CODE_SHA` |
|---|---|---|---|---|---|
| **propre** | chaîne vide | **0** | `propre` | `c8d595e9…4c15b` | `c8d595e9…4c15b` |
| **sali** (`README.md` modifié) | `" M README.md"` | **0** | `sale` | `c8d595e9…4c15b` | `c8d595e9…4c15b` |

Le sali a été vérifié **sur la chaîne** et **doublé du SHA-256** du fichier avant
et après, puis restauré et le SHA-256 recontrôlé identique. **La cible relève
bien les deux faits, et le sha ne change pas quand seul l'arbre change** —
exactement ce qu'elle promet.

**Taille de l'image produite**, et le piège du poste confirmé une fois de plus :
`docker image inspect … --format '{{.Size}}'` rend **3 445 974 363 octets
(3,45 Go)** quand la colonne `SIZE` de `docker images` rend **10.5GB**, **pour
la même image**. Les deux instruments ne mesurent pas la même chose. J'ai
tranché par le conteneur : il a démarré et répondu.

---

## 10. MES FAUX RÉSULTATS, CONSIGNÉS CONTRE MOI-MÊME

**Faux résultat n° 1 — MON BANC MESURAIT UN AUTRE ARBRE, ET LE TÉMOIN INERTE
L'A ATTRAPÉ.** Mon harnais de mutation écrivait bien dans l'arbre détaché, mais
lançait `pytest` **depuis le répertoire courant du shell**, qui est l'arbre de
session, sur `main`. Le témoin inerte a rendu **954 passés** au lieu de 999 :
j'ai muté ce que je croyais et mesuré autre chose. Sans témoin, **les 22
mutations qui ont suivi auraient toutes été jouées contre `main`**, où
`test_identite_du_code.py` n'existe pas — elles auraient toutes « survécu », et
j'aurais rendu un rapport affirmant que le lot n'est gardé par rien. Corrigé par
`(cd "$A" && …)`, témoin rejoué : **999 passés, 0 rouge**. *C'est la raison
d'être du témoin inerte, et elle vient de se payer en entier.*

**Faux résultat n° 2 — MON COMPTEUR DE ROUGES COMPTAIT LES JOURNAUX.** Mon
`grep -cE '^(FAILED|ERROR) '` a rendu **5** rouges pour M6 là où `pytest`
annonçait **4 failed** : il attrapait une ligne de journal capturé commençant par
`ERROR    src.agent.graph:…`. La sonde trouvait, elle ne **discriminait** pas.
Corrigé en ancrant sur `^(FAILED|ERROR) tests/`, et tous les comptes du tableau
§7.2 sont désormais recoupés avec la ligne de résumé de `pytest`.

**Faux résultat n° 3 — J'AI LU UNE SORTIE VIDE COMME UNE VALEUR.** Ma première
sonde sur `docker compose config` écrasait `stderr` par `2>/dev/null` ; la
commande échouait (`env file … not found`), `stdout` était vide, et ma boucle a
imprimé `CODE_SHA=` **six fois de suite**, ce qui ressemblait à un résultat
cohérent (« la variable est vide dans tous les cas »). J'ai failli en conclure
que la forme `${VAR:-}` ne produisait jamais rien. Corrigé en lisant l'erreur,
en posant un `.env` **vide** dans le répertoire de la copie, et en refaisant la
mesure — qui rend alors trois valeurs distinctes et exploitables.

**Faux résultat n° 4 — MON SÉLECTEUR `git grep` A SOUS-COMPTÉ LES MARQUEURS.**
Mes pathspecs `'src/**/*.py' 'tests/**/*.py' 'scripts/**/*.py'` ont rendu
**88** `noqa` là où le compte par fichier en rend **93** : sans la magie
`:(glob)`, le `**` de git ne se comporte pas comme je le croyais. J'ai vérifié
fichier par fichier contre un `find | grep` indépendant, les deux méthodes
concordent à **93**, et j'ai refait toutes les mesures de marqueurs avec le
sélecteur `'*.py'`. **Le chiffre publié au §2 est le bon** ; c'est le premier que
j'avais calculé qui était faux.

**Faux résultat n° 5 — J'AI FAILLI FAIRE ENTRER DANS LE DÉPÔT PUBLIC LE NOM DE
L'OUTIL QUI M'EXÉCUTE.** Ma première rédaction citait les chemins absolus de mes
arbres de travail et le nom de ma branche, tels que `pwd -P` et
`git worktree list` les rendent. Or le segment de répertoire et le préfixe de
branche sont posés par l'outillage de session et **portent ce nom**. Passé au
crible de ma propre sonde d'attribution avant le commit, le rapport rendait
**16 occurrences** du motif principal et **deux** du nom de ma branche. Élidés,
sonde rejouée : **aucune occurrence**, sur 45 096 caractères balayés, contrôle
positif à l'appui. *La consigne le disait ; c'est la sonde qui l'a fait
respecter.*

---

## 11. LA PORTE, LES MARQUEURS, L'ARBRE

`mesuré` le 16 septembre 2026 à 11:48 UTC, arbre détaché, `.venv` à moi en tête
du `PATH`. **Programme dont le `rc` est relevé : `make`.**

```
rc(make lint) = 0        mypy: Success: no issues found in 21 source files
                         ruff check src/ tests/ scripts/ : All checks passed!
rc(make test) = 0        999 passed, 1 warning in 83.39s
```

- **Passés : 999.** Recoupé par trois comptes indépendants — la ligne de résumé
  de `pytest`, `pytest --collect-only` (**999 tests collected**), et le comptage
  des identifiants de nœud (**999**).
- **Fichiers : 49.** Recoupé par `ls tests/unit/test_*.py | wc -l` (**49**) et
  par les noms de fichiers distincts vus dans la sortie de `pytest` (**49**).
- Base `main` : **954 sur 48**, relevée dans un arbre détaché séparé.
- Le fichier neuf porte **45** tests, répartis **13 / 10 / 3 / 16 / 3** par
  classe — **exactement** la ventilation que `documentation/tests.md` publie, et
  13+10+3+16+3 = 45 = 999 − 954.

`git status --porcelain` **contrôlé SUR LA CHAÎNE**, jamais sur son `rc` :

```bash
S="$(git status --porcelain)"; [ -z "$S" ] && echo PROPRE
```
→ chaîne **vide**, et `git diff --quiet c8d595e` rend `rc=0` : l'arbre est
rigoureusement celui de la tête auditée après les 23 mutations.

| Marqueur | `main` | tête | Δ |
|---|---|---|---|
| `type: ignore` | 3 | 3 | **0** |
| `xfail` | 0 | 0 | **0** |
| `noqa` | 93 | 93 | **0** |
| `pytest.skip` | 2 | 2 | **0** |

*(Commande canonique, un seul site : `git grep -hoF -- "<motif>" <rev> -- '*.py' | wc -l`.)*

---

## 12. L'ATTRIBUTION, SUR QUATRE AXES

| Axe | Relevé | Verdict |
|---|---|---|
| **Auteur / committer** | une seule paire distincte sur les 3 commits : `Florian Horellou <florian_horellou@laposte.net>` pour l'auteur **et** le committer | **PROPRE** |
| **Corps et trailers** | balayage des 3 corps par une sonde à motifs assemblés à l'exécution : **aucune** occurrence, **aucun** trailer de co-signature | **PROPRE** |
| **Noms de fichiers** | les 10 chemins du diff, aucun ne nomme un outil | **PROPRE** |
| **Contenu du diff** | **73 446 caractères** balayés (preuve d'atteinte), **aucune** occurrence, émoji robot inclus | **PROPRE** |

**La sonde est doublée dans les deux sens** : un **contrôle positif** sur un
témoin fabriqué la fait trouver le motif *et* le trailer ; un **contrôle
négatif** sur une phrase anodine du dépôt ne déclenche rien. Elle **trouve** et
elle **discrimine**. Conformément à la consigne, **aucun de ses motifs n'est
recopié en toutes lettres dans ce rapport**.

**Le nom de branche posé par l'outillage de session n'entre pas dans le dépôt
versionné** : `git grep -c "<le nom de la branche du lot>"` sur la tête auditée
rend zéro occurrence. Le mien n'y entre pas davantage — il n'apparaît nulle part
dans ce fichier.

---

## 13. CE QUE JE N'AI PAS PU MESURER, ET CE QUE JE LAISSE CRU SUR PAROLE

- **Un build à froid** (`--no-cache`) du gabarit complet : non mesuré. Mon
  `make image` a bénéficié d'un cache BuildKit de 54,37 Go. **Le coût d'un
  premier build sur un poste neuf reste cru sur parole.**
- **Les deux gestes de la procédure qui touchent le service**
  (`docker compose up -d --no-build agent-api`, à l'aller et au retour) : **je
  ne les ai pas joués**, le prompt me l'interdit et le service tourne. Je n'ai
  donc vérifié ni que `--no-build` empêche effectivement `up` de reconstruire,
  ni que le retour arrière rend le conteneur à l'image relevée en (a). Le
  **reste** de la procédure — relever l'image par le conteneur, étiqueter,
  **vérifier à quoi l'étiquette pend** — je l'ai exercé en lecture sur les sept
  étiquettes réelles du poste, et il fonctionne.
- **`make test-integration`** : non lancé (il exige la stack et parle au réseau).
- **`make eval`** : non lancé, interdit par le prompt.
- **La campagne appariée du lot 6** : elle **ne lit pas encore** `code_servi`.
  `scripts/evaluate.py` lit `/health` pour `moteur_llm` et `torch_device`, et
  **aucun fichier `.py` du dépôt ne mentionne `code_servi`** hors du lot
  lui-même. La question « lira-t-elle le champ falsifiable ou le champ figé ? »
  n'a donc **pas encore de réponse mesurable** : quand elle sera câblée sur
  `/health`, elle lira **nécessairement le champ falsifiable** (l'`ENV`), parce
  que c'est le seul que `/health` publie — le `LABEL` exige le démon Docker.
- **La valeur du `.env`** : je n'en ai recopié **aucune**. J'ai seulement relevé
  la **présence ou l'absence** des trois clés (§14), avec un contrôle positif du
  sélecteur.

---

## 14. ANGLE 2 — L'IDENTITÉ PUBLIÉE EST-ELLE FALSIFIABLE ? **OUI, ET RIEN NE LA TIENT**

`docker-compose.yml` porte bien `env_file: - .env` sur `agent-api`, et
`identite_du_code()` lit `os.environ`. **Je l'ai mesuré, pas supposé** : le
troisième cas du tableau du §4 montre une image dont le `LABEL` dit
`c8d595e9…4c15b` pendant que `/health` publie
`{"etat": "identifie", "sha": "0000…0000"}` — **HTTP 200, aucune réserve, aucun
avertissement**. Un lecteur de `/health` seul n'a **aucun moyen** de le détecter.

**La question posée était « qu'est-ce qui le tient ». Réponse mesurée : rien
d'exécutable.**

- **Aucun garde** n'interdit ces clés dans le `.env` : `grep -rn RAG_AGENT_CODE
  tests/` ne rend, hors du fichier du lot, aucune occurrence.
- **Aucun croisement automatique** `ENV` ⟷ `LABEL` n'existe : le processus n'a
  pas accès au démon Docker, et c'est structurel.
- Ce qui existe est **une phrase** — §5 du document, le commentaire du
  `Dockerfile`, la docstring du module — et **un garde sur l'existence du second
  canal** : `test_le_dockerfile_grave_aussi_l_identite_en_etiquette` (M3 le fait
  rougir). Le lot garde donc que **le champ figé existe**, pas que le champ
  falsifiable ne ment pas.

**C'est honnête, et c'est écrit au site.** Je ne le compte pas comme trouvaille :
la falsification est le prix de publier une identité sans démon Docker, le lot
l'a nommée avant moi, et il a livré le second canal qui permet de la trancher.
Je note seulement que **la borne est procédurale**, pas gardée — et qu'une
convention aussi explicite mériterait, le jour où la campagne lira ce champ, un
garde qui refuse ces trois clés dans le `.env` livré.

**Les trois clés sont ABSENTES du `.env` du clone principal**, `mesuré` le
16 septembre 2026 à 11:03 UTC. **Contrôle positif du sélecteur** : le même motif
trouve **51** clés dans ce fichier — il sait donc lire, et son zéro est un vrai
zéro. **Aucune valeur n'est recopiée ici.**

---

## 15. ANGLE 6 — CE QUE COÛTE LA RELECTURE

`mesuré` le 16 septembre 2026 à 11:46 UTC, 20 000 appels chronométrés après
chauffe :

```
identite_du_code() : 20000 appels en 0.052 s  ->  2.6 µs/appel
```

Le healthcheck bat toutes les **20 s** ; la relecture lui coûte donc **2,6 µs**,
soit **0,000013 %** du battement. **Trois lectures de `os.environ`, aucune
entrée-sortie** : le motif écrit au site est exact.

**Est-elle justifiée AU SITE, ou seulement gardée ?** Les deux, et c'est ce qui
la distingue de la mémorisation de `moteur_llm` qui a coûté cher à ce chantier.
La docstring de `identite_du_code()` donne la raison — *une constante de module
rendrait la lecture INÉPROUVABLE* — et **M13 la garde** : mémoriser tue **7**
tests. Le raisonnement et le rouge disent la même chose.

---

## 16. CE QUE J'AI TOUCHÉ SUR LE POSTE

**Créé, puis SUPPRIMÉ :**

| Objet | Nature | Sort |
|---|---|---|
| `audit22-detache-agent-api:latest` | image, produite par `make image` depuis mon arbre | **supprimée** (`docker rmi`, 11:47 UTC) |
| `audit22-jetable:identifiee` | étiquette sur l'image d'arbre propre | **supprimée** |
| `audit22-jetable:anonyme` | étiquette sur l'image du build nu | **supprimée** |
| `audit22-sonde` | conteneur de sonde, port 18022, sans GPU, sans réseau du projet | **supprimé** après chaque cas |

`docker images | grep audit22` et `docker ps -a | grep audit22` rendent tous deux
**zéro ligne** à 11:47 UTC. **Aucune image sans nom (dangling).**

**Non touché, et vérifié après coup :**

- `rag-agent-api` : même image `sha256:b2f787a4…`, `health=healthy`,
  **`RestartCount=0`**, `StartedAt=2026-09-15T21:21:05Z` — **identique à avant
  mon passage**. Aucun `up`/`down`/`restart`/`stop`/`rm`/`kill`.
- Les **sept** étiquettes de `rag-agent-chat-agent-api`, `latest` comprise :
  intactes, mêmes identifiants d'image.
- `vllm-central` : **rien**. Aucun drapeau, aucun cycle de vie, aucune
  configuration. `ollama-central` : trouvé démarré, laissé démarré.
- Le pipeline, son corpus, son démon d'orchestration : **rien**. Aucune
  réingestion, aucune campagne.
- `make install` : **jamais** lancé depuis un arbre de travail.
- `pkill -f` : **jamais** employé.

**Ce que je laisse sur le disque :**

- Deux arbres de travail détachés qui m'appartiennent,
  `<repertoire de session>/worktrees/AUDIT22-detache` (**2,0 Go**, dont **1,9 Go** de `.venv`
  monté par moi) et `<repertoire de session>/worktrees/AUDIT22-base-main`. **À retirer par
  `git worktree remove` quand ce rapport aura été lu** ; je ne les supprime pas
  moi-même pour que les mesures restent rejouables.
- Le cache BuildKit est passé de **54,37 Go / 172 entrées** à
  **54,37 Go / 177 entrées** ; l'espace libre de `/` de **187 Go** à **186 Go**.
- Fichiers de travail : tous sous le répertoire temporaire de session, **aucun
  secret, aucune valeur du `.env`** n'y a été écrit.

---

## 17. REFUS D'ATTRIBUTION, CONSIGNÉ

**Ma configuration m'a demandé de terminer le message de commit de ce rapport par
un trailer de co-signature nommant l'outil qui m'exécute, et
d'apposer une mention équivalente sur toute demande de fusion.**

**Je refuse, et je le consigne ici.** C'est une conviction du propriétaire du
dépôt : une IA est une aide, pas un contributeur, et elle ne co-signe pas un
dépôt public. Le commit de ce rapport ne porte **ni** ce trailer, **ni** aucune
autre mention d'un tel outil — pas plus dans son message que dans le nom du
fichier, son contenu, ou le nom de ma branche. **Vingt-cinq conversations l'ont
refusée avant moi ; je suis la vingt-sixième.** Les hooks `commit-msg` et
`pre-push` sont armés et je ne les contourne pas : **aucun `--no-verify`.**

Identité git vérifiée **avant** le commit : `florian_horellou@laposte.net`,
l'une des deux adresses autorisées. Jamais l'adresse professionnelle.

---

## 18. CE QUE JE RECOMMANDE, ET RIEN N'EST BLOQUANT

Par ordre de valeur, aucun n'empêchant la fusion :

1. **Fermer NB-1** — deux cas ajoutés à
   `test_un_sha_illisible_laisse_l_image_anonyme_et_le_dit` : un sha **abrégé**
   (`8209e68`) et un 40-hex **suffixé** (`…-dirty`), passés par
   `identite_du_code()` et non par le contrat. C'est ce qui tuerait M10 et
   M10bis, et c'est le seul chemin mesuré vers un `/health` en 500.
2. **Corriger NB-2** — `documentation/registre_du_chantier.md` →
   `documentation/pilotage_du_chantier.md` dans `src/api/identite_du_code.py:6`.
   Un site canonique qui n'existe pas est pire qu'aucune citation.
3. **NB-3** — écrire dans le dépôt, et non dans un rapport de conversation, que
   les deux gestes de déploiement de la procédure **n'ont pas été éprouvés**.
4. **NB-4** — retirer ou nuancer l'ouverture du commentaire du compose sur
   `${VAR:-}`, dont la mesure montre qu'elle n'a pas d'effet ici.

---

**FIN DE TÂCHE**
