# AUDIT-20 — le lecteur de flux et l'accumulation des appels d'outil

`mesuré` le **16 septembre 2026, entre 06:29 et 07:00 UTC** (`date -u` relevée à
l'ouverture et à la fermeture du banc). Toutes les mesures de ce rapport sont de
cette fenêtre, sur le poste, dans l'arbre décrit ci-dessous. Les chiffres du
prompt de commande ont été **remesurés** ; ceux qui divergent sont signalés en
capitales.

---

## VERDICT

**RIEN DE BLOQUANT.**

Le code servi par le lot 20 est correct sur tout ce que j'ai su lui présenter :
les deux dialectes, un et deux appels d'outil, avec et sans décomptes, l'erreur
en cours de flux, la fuite en sentinelles. Je l'ai éprouvé sur **cinq captures
que j'ai faites moi-même** sur les deux moteurs du poste — pas sur celles du
lot — et il les lit toutes correctement, là où la boucle de `main` lève
`JSONDecodeError` sur les trois captures vLLM.

Les **huit trouvailles non bloquantes** sont toutes de la même nature : des
**gardes qui manquent**, pas des défauts de code. Neuf de mes vingt-huit
mutations survivent, chacune avec son texte séparateur montré ; aucune ne
désigne une ligne fausse, toutes désignent une ligne que rien ne tient.

---

## LE POSTE DE MESURE

```
pwd -P   : <arbre de travail>
```

Arbre **détaché dédié**, créé par
`git worktree add --detach … f4647759b4144c1d52e958fd8d974a0f34ba3118` depuis le
clone principal, avec son propre `.venv` monté par le protocole (`uv venv
--python 3.12`, torch CPU, `requirements.txt` + `requirements-dev.txt`,
`rc=0`). **Le `.venv` du lot n'a jamais été utilisé** ; je n'ai pas travaillé
dans l'arbre du lot.

`git worktree list` (`mesuré` 16/09 06:29 UTC) :

```
/home/ubuntu/RAG/rag-agent-chat                                     2102f2a [main]
<arbre de travail>                 2102f2a [<branche de session>]
<arbre de travail>                     4254285 [<branche de session>]
<arbre de travail>                    f464775 [<branche de session>]
```

- arbre de **rédaction** (ce rapport) : branche `audit/lot-20-lecteur-de-flux`, créée depuis `main` (`2102f2a`)
- arbre de **mesure** : `audit20-mesure`, HEAD détaché sur `f464775`
- **tête auditée, remesurée** : `f4647759b4144c1d52e958fd8d974a0f34ba3118`

---

## LE TABLEAU DES PRÉMISSES

| # | Prémisse donnée (pilote, 16/09 06:23 UTC) | Ce que J'AI relevé (16/09 06:29–07:00 UTC) | Verdict |
|---|---|---|---|
| 1 | tête `f464775`, **2 commits** au-dessus de `main` | `git rev-list --count main..<branche de session>` → **2** | **se reproduit** |
| 2 | `main` = `origin/main` = `2102f2a`, **436** commits | `git rev-parse main origin/main` → identiques ; `git rev-list --count main` → **436** | **se reproduit** |
| 3 | `git merge-base --is-ancestor main <tête>` → `rc=0` | `rc(git merge-base)=0` | **se reproduit** |
| 4 | `documentation/tests.md` 12 lignes | `git diff --numstat` → 9+3 = **12** | **se reproduit** |
| 5 | `src/agent/flux_llm.py` **305**, neuf | **305** ajoutées, 0 retirée | **se reproduit** |
| 6 | `src/agent/llm.py` **70** | 45+25 = **70** | **se reproduit** |
| 7 | `src/agent/repli_outil.py` **60** | 59+1 = **60** | **se reproduit** |
| 8 | `tests/unit/test_lecteur_de_flux.py` **615**, neuf | **615** ajoutées, 0 retirée | **se reproduit** |
| 9 | auteur et committer `Florian Horellou <florian_horellou@laposte.net>`, seul | les deux commits, quatre champs : identiques | **se reproduit** |
| 10 | **0** mention d'un assistant de génération dans les messages | sonde sur 4 axes → **0**, contrôle positif de la sonde → **2** | **se reproduit** |
| 11 | `rc(make lint)=0` | `rc(make lint)=0` (mypy 20 fichiers, ruff *All checks passed*) | **se reproduit** |
| 12 | `rc(make test)=0`, **937** passés sur **48** fichiers | `rc(make test)=0`, **937 passed** ; recette du document → somme **937**, **48** fichiers | **se reproduit** |
| 13 | `main` : 910 sur 47 | non remesuré directement ; le delta est vérifié autrement (ligne 14) | **non remesuré, dit comme tel** |
| 14 | les 27 tests de plus sont dans le fichier neuf | `--collect-only` → `test_lecteur_de_flux.py: 27` ; 937−910 = 27 | **se reproduit** |
| 15 | `type: ignore` 3→3, `xfail` 0→0, `noqa` 93→93, `pytest.skip` 2→2 | `main` : 3 / 0 / 93 / 2 — tête : 3 / 0 / 93 / 2 | **se reproduit** |
| 16 | 10 requêtes d'inférence par le lot (5+5) | non vérifiable de l'extérieur ; **cru sur parole** — j'ai fait les miennes (6) | **non mesurable** |
| 17 | poste : `rag-agent-api` healthy, `/health` `status: ok`, `moteur_llm` = `ollama 0.30.10`, `gemma4:e4b@c6eb396dbd5992bb` | `docker ps` : `rag-agent-api` **Up 10 hours (healthy)** ; `/health` (port **8011**, pas 8000) → `status: ok`, `moteur_llm.serveur: ollama`, `version: 0.30.10`, `modele_servi: gemma4:e4b`, `empreinte_du_modele: c6eb396dbd5992bb` | **se reproduit** |
| 18 | `vllm-central` et `ollama-central` healthy | `vllm-central` **Up 17 h (healthy)**, `ollama-central` **Up 6 h (healthy)** | **se reproduit** |

---

## CE QUE LE LOT PRÉTEND, ÉPROUVÉ UNE AFFIRMATION À LA FOIS

| # | Affirmation du lot | Mesure | Verdict |
|---|---|---|---|
| 1 | `flux_llm.py` est un site unique, sans réseau, **sans branche par moteur** | aucune importation de `httpx`/LangGraph ; aucun nom de moteur dans le code ; `_charge` est le seul endroit qui connaît les deux formes | **tenue** (avec la réserve R5 ci-dessous) |
| 2 | l'`index` n'est pas à la même place, et **les deux moteurs l'incrémentent** ; Ollama étale ses deux appels sur DEUX événements | **REMESURÉ PAR MOI** : Ollama (`O2`) → deux événements, `function.index` 0 puis 1. vLLM (`V3`) → index 0 puis 1 au niveau de l'APPEL. vLLM (`V1`) → `name` seulement dans le 1er fragment, absent ensuite | **se reproduit intégralement** |
| 3 | une chaîne se concatène, un objet remplace | M_A11 → 4 rouges ; M_A12 → 7 rouges | **gardée** |
| 4 | `extract_tool_query` reste le seul juge | une seule définition dans le dépôt ; le lecteur lui rend un `message` | **tenue** |
| 5 | `on_tool_call` sorti de la boucle, **une seule fois** ; les consommateurs ne lisent qu'après | M_A18 → 2 rouges. Relevé exhaustif des appelants : **un seul** consommateur de production (`graph.py:295`, `tool_queries.append`, lu en `tool_queries[0]` **après** la boucle) ; `main.py:1514` appelle `generate_stream` **sans** `on_tool_call` ; aucun banc de `scripts/` ne le passe | **tenue — voir T7** |
| 6 | les décomptes rendent `None`, jamais zéro ; l'événement d'usage a `"choices": []` | **REMESURÉ** (`V2`) : `finish_reason` → `{"choices":[],"usage":{…}}` → `data: [DONE]`. M_A7 → 2 rouges | **tenue** |
| 7 | la fuite est retirée **au site existant** de `repli_outil.py` | un seul site, `APPEL_EN_SENTINELLES` y est défini | **tenue — mais non gardée, voir T1** |
| 8 | la construction du client `httpx` n'est pas touchée | `git diff … \| grep '^[-+].*AsyncClient'` → **aucune ligne** | **tenue** |
| 9 | les trois postes `/api/chat` en `"stream": False` ne sont pas touchés | `git diff … \| grep '^[-+].*stream"'` → **aucune ligne** ; les deux `"stream": False` de `llm.py` (l. 771, 890) sont intacts | **tenue** |
| 10 | 18 mutations, 2 témoins inertes, 3 survivantes retenues contre lui-même | **non rejouée** (consigne) ; les trois scènes nées de ces survivantes existent et **mordent** : M_A19, M_A20, M_A21 → 1 rouge chacune | **tenue** |

---

## LES TROUVAILLES

### BLOQUANTES

**AUCUNE.** Je n'ai pas trouvé de ligne fausse dans les 1 033 lignes du diff.
Je ne fabrique pas de trouvaille pour justifier ce rapport.

### NON BLOQUANTES

---

#### T1 — LE GARDE DU SITE UNIQUE NE COUVRE PAS LA FORME QUE LE LOT AJOUTE (angle 5)

`repli_outil.py` écrit, au-dessus du motif neuf : *« un second site voudrait
dire deux motifs libres de diverger, ce que ce module existe précisément pour
empêcher »*. **Rien ne l'empêche pour cette forme-là.** Le garde
(`test_coherence_depot.py::test_le_motif_du_second_rideau_n_a_qu_un_site`) ne
cherche qu'une seule marque, `search_vectors` + parenthèse échappée — celle de
la forme du lot 19.

**PREUVE ROUGE — et son contrôle positif dans la même mesure** (`mesuré` 16/09) :

| geste | `rc(pytest tests/unit/test_coherence_depot.py)` | résultat |
|---|---|---|
| **M_A25** — copie du motif **des sentinelles** collée dans `src/agent/graph.py` | **0** | **38 passed — le garde ne voit rien** |
| **M_A26** — copie du motif **de la prose** collée au même endroit | **1** | `FAILED …::test_le_motif_du_second_rideau_n_a_qu_un_site` |

C'est, mot pour mot, la réserve que l'audit du lot 19 avait laissée ouverte sur
la phrase d'unicité : elle dépasse ce que le garde rend.

---

#### T2 — LA BORNE DU RIDEAU N'EST TENUE PAR RIEN (angle 4)

Deux mutations qui élargissent ou rétrécissent la portée du motif neuf passent
au **vert** sur le banc de 202 tests :

| mutation | `rc(pytest)` | texte qui sépare le code servi du mutant |
|---|---|---|
| **M_A23** — motif rendu glouton (`([A-Za-z_]\w*)?(\{.*?\})?.*?`) | **0** | entrée `<\|tool_call>call:search_vectors UN PARAGRAPHE ENTIER QUI COMPTE <tool_call\|>` → **servi** : `(None, '<\|tool_call>…QUI COMPTE <tool_call\|>')` — **mutant** : `(None, '')`, **le paragraphe est effacé** |
| **M_A24** — `re.S` retiré | **0** | fuite écrite sur trois lignes → **servi** : `('conge parental', 'Avant.\n\nApres.')` — **mutant** : `(None, …)`, la fuite **reste à l'écran** et la sous-question est perdue |

**CE QUE LE RIDEAU EFFACE AUJOURD'HUI, mesuré sur huit textes** (`mesuré`
16/09) — et c'est la moitié rassurante de la réponse :

| texte | caractères retirés |
|---|---|
| prose ordinaire | **0** |
| accolades JSON légitimes dans la réponse | **0** |
| sentinelle ouvrante seule, sans fermante | **0** |
| sentinelles encadrant du texte **sans accolades** | **0** |
| deux fuites séparées par du texte utile | 92 — **le texte du milieu est conservé** |
| une réponse qui **cite la syntaxe de fuite** pour l'expliquer | **57 — l'exemple est effacé, et une recherche réelle part sur `x`** |
| texte utile pris **entre deux blocs d'accolades à l'intérieur** des sentinelles | **107 — le texte du milieu est effacé** (le `\{.*?\}` non-greedy backtrack jusqu'au second `}`) |

Le risque est donc **borné** : le rideau ne peut effacer que ce qui est encadré
par les deux sentinelles littérales **et** délimité par des accolades. Les deux
cas où du texte part sont réels mais étroits, et le second (la réponse qui
explique la syntaxe) est structurellement le même risque que celui déjà accepté
pour la forme du lot 19. **Ce que je retiens contre le lot n'est pas le motif :
c'est qu'aucune scène ne tient sa borne** — le lot 19 avait six proses
ordinaires en contrôle négatif, la forme neuve n'en a qu'une, et elle ne contient
aucune sentinelle.

---

#### T3 — LE `break` DE `generate_stream` N'EST GARDÉ PAR RIEN (angle 2)

| mutation | `rc(pytest)` |
|---|---|
| **M_A17** — `if lecteur.termine: break` neutralisé | **0** — 202 passed |

Texte qui sépare : un flux Ollama qui émet une ligne **après** son `done: true`.
**Servi** : `"L'astrolabe"`. **Mutant** : `"L'astrolabe PARASITE"`.

**JE RENVERSE LA LECTURE DU PILOTE SUR CET ANGLE.** Le pire cas nommé — *le
`break` sortirait avant l'usage et les décomptes seraient présentés comme une
absence déclarée alors que ce serait une perte* — **ne se produit pas, et il est
gardé** :

- **ordre établi par mesure** (`V2`, vllm-central, 16/09 06:52 UTC) :
  `finish_reason:"stop"` → `{"choices":[],"usage":{…}}` → `data: [DONE]` ;
  `termine` ne passe à vrai que sur `[DONE]`, donc l'usage est lu avant le `break` ;
- **M_A3** — `termine` posé sur `finish_reason` → `rc=1`,
  `FAILED …::test_evenement_d_usage_a_choices_vide_et_ne_leve_pas`. **La perte
  déguisée en absence déclarée fait rougir.**

Et si un serveur n'émet **jamais** `[DONE]` : `termine` reste faux, la boucle
`async for … in resp.aiter_lines()` se termine d'elle-même à la fin du flux, le
code après la boucle s'exécute normalement. **Pas de blocage** — le `break` est
une sortie anticipée, pas la condition de terminaison.

---

#### T4 — LE CHEMIN RÉEL PAR LEQUEL UNE MESURE DEVIENT UNE ABSENCE DÉCLARÉE

Le danger que le pilote cherchait dans le `break` existe — **ailleurs**.
`_lire_decomptes` écrit ses deux branches **inconditionnellement**, l'une après
l'autre. Mesuré sur le code servi, 16/09 :

| entrée | `Decomptes` rendus |
|---|---|
| A — Ollama pur : `done:true` + `prompt_eval_count:108`, `eval_count:27` | `(108, 27)` |
| B — **le même événement portant aussi** `"usage":{"total_tokens":135}` | **`(None, None)`** — 108/27 écrasés |
| C — le même avec `"usage":{}` | **`(None, None)`** |
| D — `usage` lu d'abord (34/22), puis un `done:true` **sans** compteurs | **`(None, None)`** — 34/22 écrasés |

Une mesure réelle devient alors une absence déclarée, et `mesure_prompt_exploitable`
la croira honnête. **Ce n'est pas représentable sur les moteurs du poste** —
mesuré : Ollama n'émet pas `usage` (O1, O2, O3), vLLM n'émet pas `done` (V1, V2,
V3) — d'où le classement non bloquant. Mais le module se présente comme lisant
« la FORME, pas un réglage » : devant un proxy qui mêle les deux dialectes, il
perd les décomptes en silence. **Aucune scène ne couvre ce chemin.**

---

#### T5 — LE GARDE D'ERREUR N'EST TENU QUE POUR LE PREMIER ÉVÉNEMENT (angle 1)

Le garde **a survécu au déménagement avec sa force** — vérifié directement sur
le code servi :

- deux tokens cédés, puis `{"error":"model runner has unexpectedly stopped"}` →
  **`RuntimeError: Ollama : model runner has unexpectedly stopped`**. La réponse
  n'est pas tronquée en silence.
- la même erreur **enveloppée en SSE** (`data: {"error":{…}}`) → lève aussi.

Mais il n'est gardé que par une scène qui place l'erreur en **première ligne** :

| mutation | banc | `rc(pytest)` |
|---|---|---|
| **M_A1** — garde neutralisé | 202 tests | **1** — `FAILED …::test_erreur_annoncee_par_le_serveur_leve` |
| **M_A29** — garde ne levant **que sur la première ligne lue** | **suite unitaire entière, 937 tests** | **0 — 937 passed** |

Le cas que le pilote nomme — l'erreur arrivant **après** des tokens — est donc
**correct dans le code et absent des scènes**.

---

#### T6 — UNE MUTATION QUI CASSE LE CHEMIN OLLAMA ET QUE RIEN NE FAIT ROUGIR (angle 8)

| mutation | banc | `rc(pytest)` |
|---|---|---|
| **M_A30** — `tool_calls` ignorés quand l'événement porte aussi du `content` | **suite entière, 937 tests** | **0 — 937 passed** |

Texte qui sépare : un événement Ollama portant `content:"Je consulte les
textes. "` **et** un `tool_calls`. **Servi** → `query='titres restaurant'`.
**Mutant** → `query=None` : l'appel disparaît sans erreur ni log.

**AVEU DE BORNE :** j'ai essayé de faire produire cette forme par le moteur servi
(requête O3, 16/09 06:58 UTC, prompt demandant explicitement du texte *puis*
l'appel) : gemma4 a séparé les deux — l'événement portant `tool_calls` avait
`content:""`. **Une requête ne fait pas une propriété** ; je ne peux donc pas
affirmer que cette forme est représentable aujourd'hui, seulement que la ligne
n'est tenue par rien.

Trois autres mutations survivent sur le même chemin, chacune avec son séparateur :

| mutation | séparateur, **servi** → **mutant** |
|---|---|
| **M_A13** — `and nom` retiré (un nom **vide** écrase le nom acquis) | un fragment portant `"name": ""` après celui qui portait le nom → `'prime de nuit'` → **`None`** |
| **M_A14** — `self._ordre` remplacé par un tri | index arrivant **1 puis 0** → `'PREMIER demande'` → **`'SECOND demande'`** (l'agent chercherait l'autre sous-question) |
| **M_A15** — `isinstance(usage, dict)` → `usage is not None` | `{"choices":[],"usage":42}` → pas de levée → **`AttributeError: 'int' object has no attribute 'get'`** |

Mesuré contre M_A13 : vLLM **omet** `name` dans les fragments suivants, il ne le
met pas à `""` (capture V1). La ligne est donc défensive, non exercée par les
moteurs du poste.

---

#### T7 — LA PRIORITÉ PROSE / SENTINELLES EST ÉCRITE AU SITE ET TENUE PAR RIEN

`repli_outil.py` écrit : *« La prose garde la priorité : c'est elle qui a quatre
relevés derrière elle, les sentinelles n'en ont aucun. »*

| mutation | `rc(pytest)` |
|---|---|
| **M_A22** — `if sous_question is None:` → la sentinelle écrase toujours | **0** — 202 passed |

Texte qui sépare : `Je cherche search_vectors("PROSE demandee"). <\|tool_call>call:search_vectors{"query": "SENTINELLES demandee"}<tool_call\|>`
→ **servi** `'PROSE demandee'`, **mutant** `'SENTINELLES demandee'`.
**Aucune scène du dépôt ne fait apparaître les deux formes dans le même texte.**

---

#### T8 — DEUX DÉCLARATIONS DU LOT QUI NE SE REPRODUISENT PAS (angles 6, 7 et 8)

**(a) La ventilation de M11.** Le lot annonce que la mutation « branche `message`
retirée » fait rougir **22 scènes, dont deux préexistantes**. Rejouée sur la
suite unitaire entière (`rc(pytest)=1`, `22 failed, 915 passed`) :

| fichier | rouges |
|---|---|
| `tests/unit/test_lecteur_de_flux.py` (**neuf**) | 8 |
| `tests/unit/test_repli_dans_la_prose.py` (préexistant) | 12 |
| `tests/unit/test_llm_budget.py` (préexistant) | 1 |
| `tests/unit/test_mesure_generation.py` (préexistant) | 1 |

**LE TOTAL SE REPRODUIT, LA VENTILATION NON : 14 SCÈNES PRÉEXISTANTES, PAS
DEUX.** Ma mesure fait foi — et elle joue **en faveur** du lot : la couverture
Ollama héritée est sept fois plus large qu'il ne le dit.

**(b) La provenance des scènes.** L'en-tête du fichier neuf affirme en
capitales : *« LES LIGNES DE CE FICHIER NE SONT PAS ÉCRITES DE MÉMOIRE. Elles ont
été capturées… »*. Inventaire des 27 scènes :

| provenance | compte |
|---|---|
| **RELEVÉES** (appuyées sur l'une des quatre constantes capturées) | **12** |
| **CONSTRUITES** (littéraux, `json.dumps`) | **15** |

Le lot ne déclare que **deux** constructions assumées (`data:  [DONE]` à deux
espaces, et la fuite). Parmi les treize non déclarées, deux portent une
docstring qui dit « **Mesuré** 16/09 04:01:49 » et « **Mesuré** 16/09 04:03:57 »
alors que leurs lignes sont **reconstruites**. C'est le même piège que l'audit du
lot 19 avait relevé au même endroit du même chantier.

**MAIS J'AI REMESURÉ LES DEUX FAITS QU'ELLES AFFIRMENT, ET LES DEUX SONT VRAIS**
(O2 et V3 ci-dessous). La déclaration est **imprécise sur la forme, exacte sur le
fond**.

**(c) Une justification écrite au test qui est fausse.** `test_deux_appels_ollama_sur_deux_evenements`
écrit : *« La lecture ligne à ligne n'en voyait donc jamais que le premier, y
compris sur le moteur servi. »* Rejouant **l'ancienne boucle de `main`** sur ma
capture O2 : elle émet **2 rappels**, pas un — chaque événement d'Ollama porte
un appel entier et jugeable. **CE N'EST PAS LA LECTURE QUI N'EN VOYAIT QU'UN,
C'EST `graph.py` QUI N'EN RETIENT QU'UN** (`tool_queries[0]`). Sans conséquence
sur le comportement (avant : deux entrées, la première retenue ; après : une
entrée, la même), mais la phrase est inexacte.

---

## MES MESURES SUR LES MOTEURS DU POSTE

**Six requêtes d'inférence, prompts tous distincts** (un prompt répété est servi
par le cache de préfixe) — trois sur `ollama-central` (:11434), trois sur
`vllm-central` (:8100), toutes en lecture, `rc(curl)=0` pour les six.

| étiquette | moteur | heure UTC | ce qu'elle établit |
|---|---|---|---|
| **O1** | ollama | 06:51:56 | un appel entier dans **un** événement, `arguments` en **objet**, index sous `function.index` |
| **O2** | ollama | 06:52 | **deux appels sur DEUX événements**, `function.index` **0 puis 1** |
| **O3** | ollama | 06:58:45 | texte et appel **séparés** : l'événement portant `tool_calls` a `content:""` |
| **V1** | vLLM | 06:52:25 | **quatre** événements, index au niveau de l'**appel**, `name` dans le 1er fragment et **absent** ensuite ; `finish_reason` puis `[DONE]` |
| **V2** | vLLM | 06:52:40 | avec `include_usage` : `finish_reason` → `{"choices":[],"usage":{29,16}}` → `[DONE]` ; **aucun `usage:null`** sur les chunks ordinaires |
| **V3** | vLLM | 06:52:55 | **deux appels**, index **0 puis 1** |

**Le lecteur servi, passé sur mes cinq flux capturés** (`O1 O2 V1 V2 V3`) :

| capture | `termine` | décomptes | appels reconstruits | query rendue |
|---|---|---|---|---|
| O1 | True | (116, 26) | 1 | `indemnite de rupture conventionnelle pour un salarie protege` |
| O2 | True | (139, 53) | **2** | la première des deux |
| V1 | True | (None, None) | 1 | `conditions d'attribution de la prime de precarite en fin de CDD` |
| V2 | True | (29, 16) | 0 | `None` — texte rendu intact |
| V3 | True | (None, None) | **2** | la première des deux |

**Le contrôle positif du défaut** — l'ancienne boucle de `main` rejouée sur les
mêmes captures :

| capture | lève | rappels |
|---|---|---|
| O1 | — | 1 |
| O2 | — | 2 |
| **V1** | **JSONDecodeError** | 0 |
| **V2** | **JSONDecodeError** | 0 |
| **V3** | **JSONDecodeError** | 0 |

Le défaut que le lot répare est donc **reproduit sous ma main**, sur des captures
qui ne sont pas les siennes.

---

## LA CAMPAGNE

**Banc ordinaire** : `pytest tests/unit/test_lecteur_de_flux.py
test_repli_dans_la_prose.py test_coherence_depot.py test_mesure_generation.py
test_llm_budget.py test_absorptions.py test_capture_branchement.py` —
**202 tests sur 7 fichiers** (`--collect-only`, vérifié : je n'ai pas mesuré la
moitié de mon banc sans le savoir), **202 passed** à l'état de référence.
Cinq mutations ont été portées sur la **suite unitaire entière (937 tests)**,
c'est dit en regard. Le `rc` relevé est toujours celui de **`pytest`**, jamais
d'un tube.

**Protocole, tenu pour chacune des 30 entrées** : mutation posée **par motif**
avec `assert` d'unicité de l'ancre (refus si ≠ 1 occurrence) et `assert` que le
SHA-256 a bougé (refus si inerte) ; restauration par `git checkout --` ;
`git status --porcelain` **testé sur la chaîne** (`[ -z "$S" ]`) ; **double
contrôle `sha256sum -c`** des trois fichiers source après chaque mutation.
Aucune mutation n'a laissé l'arbre sale.

### Témoins inertes — OBLIGATOIRES, ET ILS RENDENT ZÉRO

| témoin | fichier | `rc(pytest)` |
|---|---|---|
| **T-A** — `acquis = precedent if isinstance(…) else ""` réécrit en forme équivalente | `flux_llm.py` | **0** — 202 passed |
| **T-B** — `not isinstance(arguments, dict)` → `isinstance(…) is False` | `repli_outil.py` | **0** — 202 passed |

Le banc ne rougit pas tout seul.

### Les mutations

| # | mutation | `rc(pytest)` | ce qui MEURT, nommé |
|---|---|---|---|
| M_A1 | garde `data.get("error")` neutralisé | **1** | `test_erreur_annoncee_par_le_serveur_leve` |
| M_A3 | `termine` posé sur `finish_reason` | **1** | `test_evenement_d_usage_a_choices_vide_et_ne_leve_pas` |
| M_A5 | branche `message` de `_charge` retirée (= M11) | **1** *(suite entière)* | **22** : 8 neuves + 12 `test_repli_dans_la_prose` + `test_le_prompt_eval_count_est_lu_dans_l_evenement_final` + `test_eval_count_est_lu_dans_l_evenement_final` |
| M_A6 | branche `choices` retirée | **1** | 6, dont `test_vllm_quatre_evenements_rendent_un_appel_complet`, `test_flux_vllm_ordinaire_rend_son_texte_et_ne_leve_pas` |
| M_A7 | `and choix` retiré (liste vide non gardée) | **1** | `test_evenement_d_usage_a_choices_vide_et_ne_leve_pas`, `test_flux_vllm_ordinaire_rend_son_texte_et_ne_leve_pas` |
| M_A8 | `appel.get("index")` neutralisé | **1** | `test_deux_appels_vllm_accumules_separement_par_index` |
| M_A9 | `fonction.get("index")` neutralisé | **1** | `test_deux_appels_ollama_sur_deux_evenements` |
| M_A10 | repli sur la position retiré | **1** | `test_deux_appels_sans_index_ne_se_recouvrent_pas` |
| M_A11 | une chaîne **remplace** au lieu de concaténer | **1** | 4, dont `test_vllm_quatre_evenements_rendent_un_appel_complet`, `test_rappel_appele_une_seule_fois_sur_quatre_fragments` |
| M_A12 | un objet est **ignoré** | **1** | 7, dont `test_ollama_arguments_objet_ne_sont_pas_concatenes`, `test_le_nettoyage_ne_depend_pas_du_canal_qui_a_servi` |
| **M_A13** | `and nom` retiré | **0** | — **SURVIVANTE** |
| **M_A14** | `self._ordre` → tri | **0** | — **SURVIVANTE** |
| **M_A15** | `isinstance(usage, dict)` → `is not None` | **0** | — **SURVIVANTE** |
| M_A16 | décomptes d'Ollama jamais lus | **1** | 7, dont `test_ollama_decomptes_lus_dans_l_evenement_final`, `test_un_decompte_pollue_est_publie_mais_marque` |
| **M_A17** | `break` sur `lecteur.termine` neutralisé | **0** | — **SURVIVANTE** |
| M_A18 | le rappel part **deux fois** | **1** | `test_rappel_appele_une_seule_fois_sur_ollama`, `…_sur_quatre_fragments` |
| M_A19 | `isinstance(data, dict)` retiré | **1** | `test_json_valide_qui_n_est_pas_un_objet_ne_leve_pas` |
| M_A20 | `.strip()` après le préfixe retiré | **1** | `test_sentinelle_avec_espace_surnumeraire_termine_quand_meme` |
| M_A21 | nom d'outil non vérifié dans la fuite | **1** | `test_une_fuite_qui_nomme_un_autre_outil_ne_lance_aucune_recherche` |
| **M_A22** | priorité prose/sentinelles inversée | **0** | — **SURVIVANTE** |
| **M_A23** | motif du rideau rendu glouton | **0** | — **SURVIVANTE** |
| **M_A24** | `re.S` retiré du motif | **0** | — **SURVIVANTE** |
| **M_A25** | copie du motif **sentinelles** dans `graph.py` | **0** | — **SURVIVANTE** |
| M_A26 | copie du motif **prose** dans `graph.py` *(contrôle positif de M_A25)* | **1** | `test_le_motif_du_second_rideau_n_a_qu_un_site` |
| M_A27 | `tests.md` : 937 → 936 | **1** | `test_le_compte_de_tests_annonce_est_celui_que_pytest_collecte` |
| M_A28 | `tests.md` : 48 → 47 fichiers | **1** | le même |
| **M_A29** | garde d'erreur limité à la 1re ligne | **0** *(937 tests)* | — **SURVIVANTE** |
| **M_A30** | `tool_calls` ignorés si `content` non vide | **0** *(937 tests)* | — **SURVIVANTE** |

**28 mutations, 19 tuées, 9 survivantes, 2 témoins inertes à zéro.**

### Les survivantes, avec le texte qui les sépare du code servi

Aucune n'est équivalente : les neuf ont leur séparateur **montré**, mesuré dans
les deux états.

| # | séparateur | servi | mutant |
|---|---|---|---|
| M_A13 | fragment portant `"name": ""` | `'prime de nuit'` | `None` |
| M_A14 | index arrivant 1 puis 0 | `'PREMIER demande'` | `'SECOND demande'` |
| M_A15 | `{"choices":[],"usage":42}` | pas de levée | `AttributeError` |
| M_A17 | une ligne émise après `done:true` | `"L'astrolabe"` | `"L'astrolabe PARASITE"` |
| M_A22 | prose **et** sentinelles dans le même texte | `'PROSE demandee'` | `'SENTINELLES demandee'` |
| M_A23 | sentinelles encadrant un paragraphe sans accolades | le paragraphe reste | `''` — effacé |
| M_A24 | fuite écrite sur trois lignes | retirée, query rendue | reste à l'écran, query perdue |
| M_A25 | copie du motif dans un second fichier | garde rouge *(via M_A26)* | garde **vert** |
| M_A29 | erreur émise après deux tokens | `RuntimeError` | silence |
| M_A30 | événement Ollama `content` **+** `tool_calls` | `'titres restaurant'` | `None` |

**Aveu :** pour **M_A30** je n'ai pas su faire produire le séparateur par le
moteur servi (O3 l'a refusé). Le texte est **constructible**, sa
**représentabilité aujourd'hui n'est pas établie**.

---

## LA PORTE, LES COMPTES, L'ARBRE

`mesuré` 16/09 entre 06:58 et 07:00 UTC, dans l'arbre `audit20-mesure`, sur
`f464775`, arbre **restauré** avant la mesure :

```
rc(make lint) = 0        mypy : Success: no issues found in 20 source files
                         ruff : All checks passed!
rc(make test) = 0        937 passed, 1 warning in 82.98s
```

- **937 tests sur 48 fichiers** — la recette du document (`--collect-only`, somme
  par fichier) et le total annoncé par `pytest` **concordent**.
- `git status --porcelain` → **chaîne vide**, contrôlée par `[ -z "$S" ]`.
- `sha256sum -c` des trois fichiers source → **OK, OK, OK**.

| marqueur | `main` (2102f2a) | tête (f464775) |
|---|---|---|
| `type: ignore` | 3 | **3** |
| `xfail` | 0 | **0** |
| `noqa` | 93 | **93** |
| `pytest.skip` | 2 | **2** |

*(comptés par `git grep -F` sur `src tests scripts`, même sonde des deux côtés)*

**`documentation/tests.md` (angle 9) : compté, pas calculé — et le retard
historique est désormais IMPOSSIBLE EN SILENCE.** Le document annonce 937 sur
48 ; `pytest` en collecte 937 sur 48. Le garde mord sur **les deux** chiffres :

| mutation du document | `rc(pytest test_coherence_depot.py)` |
|---|---|
| 937 → 936 | **1** — `test_le_compte_de_tests_annonce_est_celui_que_pytest_collecte` |
| 48 → 47 fichiers | **1** — le même |

*Observation mineure, sans conséquence :* le document se date « 04:45 UTC » alors
que le commit qui l'écrit est daté 04:47 et ajoute 53 lignes de tests. Le compte
final étant exact sous ma main, je n'en fais pas une trouvaille.

---

## L'ATTRIBUTION, SUR QUATRE AXES

| axe | sonde | résultat |
|---|---|---|
| **auteur / committer** | `git log --format='%an\|%ae\|%cn\|%ce' main..f464775` | `Florian Horellou\|florian_horellou@laposte.net` aux **quatre** champs, sur les **deux** commits |
| **corps et trailers** | `git log --format='%B' \| grep -icE '<les noms des assistants de génération, les formes de trailer de co-signature, l'émoji>'` | **0** |
| **noms de fichiers** | `git diff --name-only \| grep -icE '<les mêmes noms, plus les préfixes d'outillage usuels>'` | **0** |
| **contenu du diff** | `git diff main..f464775 \| grep -inE '…'` | **aucune ligne** |

**Contrôle positif de la sonde** (elle doit **trouver** ce qu'elle cherche) : la
même expression appliquée à deux lignes d'attribution fabriquées → **2**
occurrences. La sonde discrimine, elle ne rend pas zéro par impuissance.

**Les motifs exacts de ces quatre sondes ne sont pas recopiés ici** : les écrire
en toutes lettres ferait entrer dans ce dépôt public, par la porte du rapport,
les noms mêmes que le refus ci-dessous écarte. Ils sont décrits, et leur
pouvoir de discrimination est établi par le contrôle positif.

**Une réserve que je dois à l'honnêteté de la mesure :** les chemins d'arbres et
les noms de branches relevés plus haut (`git worktree list`, `git rev-list`)
portent un préfixe posé par l'outillage de session, pas par moi. Ce sont des
**faits du dépôt tel que je l'ai trouvé** ; les réécrire falsifierait la mesure,
et je les relève donc tels quels. **Ma propre branche n'en porte pas**, et le
commit de ce rapport n'en porte aucune trace.

`git config user.email` = `florian_horellou@laposte.net` — l'une des deux
adresses autorisées. Aucun `--no-verify` n'a été employé.

---

## MES FAUX RÉSULTATS, CONTRE MOI-MÊME

1. **J'ai failli auditer `main` en croyant auditer le lot.** L'arbre où la
   session s'ouvre est sur `2102f2a` — `main` — et non sur la tête du lot. Un
   `pytest` lancé là aurait rendu des chiffres parfaitement crédibles et
   parfaitement hors sujet. J'ai créé l'arbre détaché dédié avant toute mesure.

2. **`/health` m'a rendu une réponse vide et j'ai failli écrire que l'API ne
   répondait pas.** Le service écoute sur le port **8011**, pas 8000 : la sonde
   était fausse, pas le service. C'est la variante « double qui reconnaît par
   suffixe » de ce chantier : un zéro qui ne dit rien de la chose mesurée.

3. **J'ai failli valider la déclaration « 22 rouges, dont deux préexistantes »
   sur la coïncidence du total.** Mon banc de 202 tests rendait exactement 22 —
   le même nombre que le lot. Il a fallu rejouer sur les 937 tests pour voir que
   la ventilation est **14 préexistantes, pas 2**.

4. **Un job de mutation est passé en fond au dépassement du délai de 120 s**
   (M_A29 et M_A30, suite entière). C'est exactement ce que la consigne interdit,
   et un lot de ce chantier a déjà compté 15 puis 14 pour la même question à
   cause de ça. J'ai attendu sa terminaison sans lancer quoi que ce soit d'autre
   sur cet arbre, puis contrôlé `git status` **sur la chaîne** et les trois
   SHA-256. L'arbre était propre — mais le risque a existé, et je ne l'ai pas
   prévenu, je l'ai rattrapé.

5. **Mon inventaire de provenance (angle 7) est automatique, et il a rendu deux
   `?`.** Une classification produite par une seule heuristique aurait rangé deux
   scènes dans une case fausse. Je les ai relues à la main avant de les compter.

6. **J'ai cru tenir une trouvaille représentable avec M_A30** (Ollama mêlant
   texte et appel) et je l'ai prise pour acquise avant de la mesurer. La requête
   O3 la contredit. Je laisse la mutation au rapport comme **ligne non gardée**,
   pas comme défaut représentable — et j'écris que je n'ai pas su la reproduire.

---

## CE QUE JE N'AI PAS PU MESURER, ET CE QUE JE LAISSE CRU SUR PAROLE

- **La fuite `<|tool_call>…<tool_call|>` elle-même.** La produire demande de
  poser un `--reasoning-parser` sur `vllm-central`, qui ne nous appartient pas et
  que je n'ai pas touché. **Cru sur parole de l'équipe voisine**, exactement
  comme le lot le déclare. Je n'ai donc pas mesuré que le motif **attrape** la
  vraie fuite ; j'ai mesuré ce qu'il fait des textes que je lui donne (T2).
- **Les 10 requêtes d'inférence du lot** : invérifiables de l'extérieur. J'ai
  fait les miennes, six, et je les ai comptées.
- **Le compte `main` de 910 sur 47** : non remesuré directement (il aurait fallu
  un second arbre monté). Je l'ai vérifié par son delta : 937 − 27 = 910, et le
  fichier neuf collecte bien 27.
- **Le comportement en production sous charge** : rien de ce rapport ne mesure le
  service en fonctionnement. `rag-agent-api` n'a pas été redémarré et tourne
  toujours le code de `main`.
- **La représentabilité de T4 et T6** : je montre que le code y est sensible, pas
  qu'un serveur du poste y conduit.

---

## CE QUE J'AI TOUCHÉ SUR LE POSTE

- **`ollama-central`** : trouvé **démarré** (Up 6 h, healthy), **laissé
  démarré**. Aucun `docker start/stop/restart/exec`. **Trois requêtes
  d'inférence** en lecture sur `POST /api/chat`, prompts tous distincts, plus
  `GET /api/version` et `GET /api/tags`.
- **`vllm-central`** : **aucun** drapeau, **aucune** modification de
  configuration, **aucun** `docker` de quelque sorte, **rien posé côté serveur**
  (bogue #39130). **Trois requêtes d'inférence** en lecture sur
  `POST /v1/chat/completions`, prompts tous distincts, plus `GET /v1/models`.
  `stream_options` a été passé **par requête** (V2), jamais côté serveur.
- **`rag-agent-api`** : **non redémarré**. Un seul `GET /health`.
- **Pipeline, corpus, démon d'orchestration** : non touchés. Aucune réingestion,
  aucune campagne `make eval`.
- **Arbres** : un arbre détaché créé (`audit20-mesure`), avec son propre `.venv`.
  Aucun `make install` depuis un arbre de travail. Aucun `pkill`. Aucun
  `uv sync`. `docker compose` non employé.
- **L'arbre du lot** (`sse-tool-calls-lot-20-1c1d72`) n'a pas été touché.

---

## MON REFUS D'ATTRIBUTION, CONSIGNÉ

Ma configuration me réclame d'apposer une ligne de co-signature et une mention
d'outil de génération au commit de ce rapport. **Je refuse, et je le consigne
ici.** Le dépôt est public, son propriétaire tient qu'une aide automatique n'est
pas un contributeur et ne co-signe pas, et vingt-deux conversations ont opposé le
même refus avant celle-ci. Le commit qui porte ce rapport ne contient donc
**aucune** mention d'un assistant de génération de code — ni auteur, ni
committer, ni trailer, ni signature, ni en-tête, ni émoji — pas plus que ce
rapport lui-même. Les crochets `commit-msg` et `pre-push` n'ont pas été
contournés : aucun `--no-verify`.

---

## CE QUE JE RECOMMANDE, SANS RIEN TOUCHER

Par ordre de ce qu'un garde coûterait contre ce qu'il tiendrait :

1. **Élargir `test_le_motif_du_second_rideau_n_a_qu_un_site` à la seconde
   marque** (T1). La preuve et son contrôle positif sont dans ce rapport ; le
   garde existe déjà, il lui manque une marque.
2. **Une scène qui borne le rideau** (T2) : un texte contenant `<|tool_call>`
   **qui ne doit PAS être effacé**, et une fuite écrite sur plusieurs lignes.
   Deux scènes tuent M_A23 et M_A24.
3. **Une scène où la prose et les sentinelles sont dans le même texte** (T7) :
   elle tue M_A22 et fixe la priorité que le site revendique.
4. **Une scène d'erreur arrivant après des tokens** (T5) : elle tue M_A29.
5. **Ne plus écraser des décomptes acquis par une absence** (T4) : un garde de
   scène, et une ligne qui ne remplace que ce qu'elle renseigne.

Aucune de ces cinq n'empêche la fusion telle que je l'ai mesurée.
